package youtube;

public interface Interfacex {

	//functional interface with single abstract method, then you can implement Lambda expression//
	//you cannot use Lambda if not a functional interface//
	public void add(int a, int b);
	
}
